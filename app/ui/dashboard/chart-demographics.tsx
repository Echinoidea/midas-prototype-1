 
export default function DemographicsChart({
  title,
  x_axis,
  y_axis,
  values,
  labels,
}: {
  title: string;
  x_axis: string;
  y_axis: string;
  values: number[];
  labels: string[];
}) {
  
}